<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/feed.xml" rel="self" type="application/atom+xml"/><link href="https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-19T11:02:44+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/feed.xml</id><title type="html">Kartik Jangid | JdeRobot x GSoC2026</title><subtitle>Kartik Jangid | JdeRobot x GSoC2026 </subtitle><entry><title type="html">Community Bonding: Weeks 1 &amp;amp; 2</title><link href="https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/blog/2026/community-bonding-weeks-1-and-2/" rel="alternate" type="text/html" title="Community Bonding: Weeks 1 &amp;amp; 2"/><published>2026-05-18T08:00:00+00:00</published><updated>2026-05-18T08:00:00+00:00</updated><id>https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/blog/2026/community-bonding-weeks-1-and-2</id><content type="html" xml:base="https://theroboticsclub.github.io/gsoc2026-Kartik_Jangid/blog/2026/community-bonding-weeks-1-and-2/"><![CDATA[<p><img src="../logo.png" alt="JdeRobot GSoC 2026"/></p> <p>I got selected for GSoC 2026 with <strong>JdeRobot</strong>, and the first two weeks have been mostly reading code, running <code class="language-plaintext highlighter-rouge">grep</code> on Dockerfiles, and slowly building a map of how this platform actually works.</p> <p>My project (#7) has three main goals. First, optimize the <code class="language-plaintext highlighter-rouge">dependencies_humble</code> Docker image through strict apt squashing and source pruning of heavy ROS 2 meta-packages — specifically <strong>Aerostack2</strong> and <strong>OMPL</strong> — which are the primary contributors to image bloat. Second, migrate the runtime to a daemonless, rootless <strong>Podman</strong> architecture for better security, including safe GPU acceleration via the <strong>Container Device Interface (CDI)</strong>. Third, restructure the build into a proper multi-stage pipeline so the final runtime image isn’t carrying compiler toolchains and build artifacts it doesn’t need. These two weeks were about understanding the codebase well enough to do all of that safely.</p> <hr/> <h2 id="the-mentor-team">The Mentor Team</h2> <p>Before I get into the work, I want to introduce the mentors I’m working with — they’ve been genuinely helpful from the start.</p> <ul> <li><strong>David Pascual-Hernández</strong> — He leads our weekly syncs, keeps the project direction on track, and is the main person I check in with when I need to validate an approach.</li> <li><strong>Javier I.</strong> — Most of the deeper architecture and code questions go to Javier. Whenever I’m confused about how something fits together in the stack, he’s the one I ask.</li> <li><strong>Md. Shariar Kabir</strong> — Official mentor for this project. Has been available throughout the bonding period and brings direct experience with the JdeRobot infrastructure.</li> <li><strong>Nikhil Gupta</strong> — official mentor, has been available for questions during the bonding period.</li> </ul> <hr/> <h2 id="week-1--setting-up-locally">Week 1 — Setting Up Locally</h2> <p>The first thing I did was clone <a href="https://github.com/JdeRobot/RoboticsApplicationManager">RADI</a> and <a href="https://github.com/JdeRobot/RoboticsBackend">RoboticsBackend</a> on my Kubuntu machine and get the legacy environment running. There was a bit of initial setup friction, but I eventually got the Django frontend routing confirmed on <strong>port 7164</strong>.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/gsoc2026-Kartik_Jangid/assets/img/local_setup-480.webp 480w,/gsoc2026-Kartik_Jangid/assets/img/local_setup-800.webp 800w,/gsoc2026-Kartik_Jangid/assets/img/local_setup-1400.webp 1400w," type="image/webp" sizes="95vw"/> <img src="/gsoc2026-Kartik_Jangid/assets/img/local_setup.png" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <figure> <picture> <source class="responsive-img-srcset" srcset="/gsoc2026-Kartik_Jangid/assets/img/JdeRobot_Gsoc_Logo.webp" sizes="95vw"/> <img src="/gsoc2026-Kartik_Jangid/assets/img/JdeRobot_Gsoc_Logo.webp" class="img-fluid rounded z-depth-1" width="100%" height="auto" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The kickoff meeting with David went well. We went through the project milestones, talked about what the refactor needs to accomplish, and set expectations for the coding period. David gave me one piece of advice early on that turned out to be useful: don’t assume this stack works like a standard web app. I took note of that, and it saved me from a wrong assumption later in the week.</p> <hr/> <h2 id="week-2--looking-at-the-dockerfile">Week 2 — Looking at the Dockerfile</h2> <p>With the environment running, I moved on to understanding why <code class="language-plaintext highlighter-rouge">Dockerfile.dependencies_humble</code> is so large. I ran a few <code class="language-plaintext highlighter-rouge">grep</code> commands directly on the file to get a concrete count.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Count all layer-altering instructions</span>
<span class="nb">grep</span> <span class="nt">-cE</span> <span class="s1">'^(RUN|COPY|ADD)'</span> Dockerfile.dependencies_humble

<span class="c"># Count separate apt-get install blocks</span>
<span class="nb">grep</span> <span class="nt">-c</span> <span class="s1">'apt-get install'</span> Dockerfile.dependencies_humble

<span class="c"># Count cache purges</span>
<span class="nb">grep</span> <span class="nt">-c</span> <span class="s1">'rm -rf /var/lib/apt/lists/\*'</span> Dockerfile.dependencies_humble
</code></pre></div></div> <table> <thead> <tr> <th>Metric</th> <th>Count</th> </tr> </thead> <tbody> <tr> <td>Total layer-altering instructions (<code class="language-plaintext highlighter-rouge">RUN</code>, <code class="language-plaintext highlighter-rouge">COPY</code>, <code class="language-plaintext highlighter-rouge">ADD</code>)</td> <td><strong>59</strong></td> </tr> <tr> <td>Independent <code class="language-plaintext highlighter-rouge">apt-get install</code> blocks</td> <td><strong>17</strong></td> </tr> <tr> <td><code class="language-plaintext highlighter-rouge">rm -rf /var/lib/apt/lists/*</code> cache purges</td> <td><strong>13</strong></td> </tr> <tr> <td>Net leaking layers</td> <td><strong>≥ 4</strong></td> </tr> </tbody> </table> <p>The proposal estimated <code class="language-plaintext highlighter-rouge">Dockerfile.dependencies_humble</code> would have around 57 <code class="language-plaintext highlighter-rouge">RUN</code>-equivalent stages. The actual count came out at <strong>59</strong> — the bloat has grown beyond even that estimate. There are 17 separate apt install commands but only 13 cache purges, meaning at least four layers have downloaded package archives permanently baked into the image. Why does this matter? Because unpurged cache layers mean students with slow internet connections are forced to download gigabytes of dead weight just to boot a basic robotics exercise. The fix is to consolidate the <code class="language-plaintext highlighter-rouge">RUN</code> stages, enforce <code class="language-plaintext highlighter-rouge">--no-install-recommends</code> everywhere, and ensure every apt chain cleans up in the same layer. On top of that, the biggest size wins will come from pruning the source installs of <strong>Aerostack2</strong> and <strong>OMPL</strong>, which pull in heavy Boost and drone-stack dependencies that the runtime image doesn’t need.</p> <hr/> <h2 id="how-code-actually-gets-executed">How Code Actually Gets Executed</h2> <p>I had assumed the Django backend (port 7164) was responsible for running student code. After tracing through the codebase, that’s not the case — Javier helped clarify this when I asked about it.</p> <p>The actual flow is:</p> <ol> <li>The React frontend packages the student’s code into a <strong>Base64-encoded ZIP file</strong>.</li> <li>That ZIP is sent over WebSockets to <strong>port 7163</strong>, which belongs to the <strong>Robotics Application Manager (RAM)</strong> — a separate daemon.</li> <li>RAM receives it, clears <code class="language-plaintext highlighter-rouge">/workspace/code/</code>, extracts the ZIP, and spawns a <strong><code class="language-plaintext highlighter-rouge">subprocess.Popen</code></strong> — either <code class="language-plaintext highlighter-rouge">python3 &lt;entrypoint&gt;</code> for Python code, or <code class="language-plaintext highlighter-rouge">colcon build</code> + <code class="language-plaintext highlighter-rouge">ros2 launch</code> for C++/ROS 2.</li> </ol> <p>Django just handles frontend routing and file writes. RAM is where execution actually happens.</p> <h3 id="--usernskeep-id-for-podman"><code class="language-plaintext highlighter-rouge">--userns=keep-id</code> for Podman</h3> <p>Because RAM uses <code class="language-plaintext highlighter-rouge">subprocess.Popen()</code>, the student’s running code inherits RAM’s UID/GID. In rootless Podman without <code class="language-plaintext highlighter-rouge">--userns=keep-id</code>, that UID gets remapped to an unprivileged one that can’t write to the bind-mounted <code class="language-plaintext highlighter-rouge">/workspace/code/</code> volume, and RAM will error out.</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Works: host UID maps into the container correctly</span>
podman run <span class="nt">--userns</span><span class="o">=</span>keep-id <span class="nt">-v</span> /workspace/code:/workspace/code ...

<span class="c"># Breaks: internal UID gets remapped, volume writes fail</span>
podman run <span class="nt">-v</span> /workspace/code:/workspace/code ...
</code></pre></div></div> <p>So <code class="language-plaintext highlighter-rouge">--userns=keep-id</code> isn’t optional for this migration — it’s a requirement based on how RAM works.</p> <h3 id="gpu-access-via-cdi">GPU Access via CDI</h3> <p>The other Podman constraint involves GPU acceleration. The legacy Docker setup uses the NVIDIA container runtime, which requires root. In rootless Podman, the equivalent is the <strong>Container Device Interface (CDI)</strong> — a standardized spec that lets Podman expose GPU devices into the container without needing a privileged daemon. The Podman migration plan has to include generating the CDI config for the NVIDIA device and passing <code class="language-plaintext highlighter-rouge">--device nvidia.com/gpu=all</code> (or equivalent) at runtime, rather than relying on the old <code class="language-plaintext highlighter-rouge">--gpus all</code> Docker flag.</p> <hr/> <h2 id="the-telemetry-bridge">The Telemetry Bridge</h2> <p>I also looked into how ROS 2 data gets from simulation topics to the browser. I expected to find <code class="language-plaintext highlighter-rouge">rosbridge_suite</code> in the dependencies. It’s not there.</p> <p>Instead, JdeRobot uses a custom Python framework. Each exercise backend spawns a native <code class="language-plaintext highlighter-rouge">rclpy</code> node, subscribes to local simulation topics like <code class="language-plaintext highlighter-rouge">/webgui/user_map</code>, and pushes the data over WebSockets on <strong>port 2303</strong> to the React <code class="language-plaintext highlighter-rouge">CommsManager</code>.</p> <p>One thing I need to be careful about during the multi-stage build: there’s a <strong>custom-patched <code class="language-plaintext highlighter-rouge">websocket_server.py</code></strong> sitting in the RAM source tree. It fixes an <code class="language-plaintext highlighter-rouge">OPCODE_CONTINUATION</code> bug in the upstream library. If we just run <code class="language-plaintext highlighter-rouge">pip install websocket-server</code> in the new runtime container, that patch gets overwritten by the standard package, and the telemetry pipeline breaks. This file has to be explicitly preserved when we split the build stages.</p> <hr/> <h2 id="camera-feeds-and-wayland">Camera Feeds and Wayland</h2> <p>One thing I was uncertain about was whether migrating from X11/TurboVNC to Wayland would break the drone camera feeds. After tracing through the vision pipeline, the answer is: it depends on which part of the frontend you’re talking about.</p> <p>The primary drone camera feeds work like this:</p> <ol> <li><code class="language-plaintext highlighter-rouge">HAL.py</code> subscribes to <code class="language-plaintext highlighter-rouge">sensor_msgs/Image</code> topics like <code class="language-plaintext highlighter-rouge">/drone0/frontal_cam/image_raw</code>.</li> <li>The Python GUI thread grabs the numpy arrays, encodes them as JPEG via <strong>OpenCV</strong>, and converts them to a Base64 JSON payload.</li> <li>That payload goes to port <strong>2303</strong> (the CommsManager).</li> <li>The React frontend puts the Base64 string directly into an <code class="language-plaintext highlighter-rouge">&lt;img&gt;</code> tag.</li> </ol> <p>That chain has no dependency on the virtual display stack — no <code class="language-plaintext highlighter-rouge">Xvfb</code>, no VNC. So for those feeds specifically, the Wayland migration doesn’t change anything.</p> <p>However, the official architecture diagram shows a different story for the Gazebo simulator itself: the web frontend includes <strong>VNC viewers</strong> (<code class="language-plaintext highlighter-rouge">Visores VNC</code>) that connect directly to the Gazebo simulation window. That part of the platform does rely on the VNC pipeline, and any X11-to-Wayland transition will need to account for it. The core camera feeds are safe; the full Gazebo desktop view is not.</p> <hr/> <h2 id="summary">Summary</h2> <p><strong>Week 1:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Cloned RADI and RoboticsBackend on local Kubuntu</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Spun up the legacy environment</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Verified Django frontend routing on port 7164</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Kickoff sync with David Pascual-Hernández</li> </ul> <p><strong>Week 2:</strong></p> <ul class="task-list"> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Ran static analysis on <code class="language-plaintext highlighter-rouge">Dockerfile.dependencies_humble</code> — 59 layers, 17 apt blocks, 13 purges</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Traced the execution pipeline: Django → RAM → <code class="language-plaintext highlighter-rouge">subprocess.Popen</code></li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Confirmed <code class="language-plaintext highlighter-rouge">--userns=keep-id</code> is required for rootless Podman</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Found the custom-patched <code class="language-plaintext highlighter-rouge">websocket_server.py</code> that must survive the multi-stage build</li> <li class="task-list-item"><input type="checkbox" class="task-list-item-checkbox" disabled="disabled" checked="checked"/>Confirmed camera feeds bypass X11 — Wayland migration won’t break them</li> </ul> <hr/> <h2 id="whats-next">What’s Next</h2> <p>Next week is <strong>Week 1 of the Coding Period</strong>. The main task is drafting the multi-stage Dockerfile split — separating the build environment (Stage 1) from the runtime environment (Stage 2), and writing the <code class="language-plaintext highlighter-rouge">COPY --from=builder</code> instructions to carry only the compiled <code class="language-plaintext highlighter-rouge">install/</code> directories across. The analysis from these two weeks gives a clear enough picture to start writing actual Dockerfiles.</p> <hr/> <p><em>Part of my GSoC 2026 work with JdeRobot. Project tracked at <a href="https://github.com/TheRoboticsClub/gsoc2026-Kartik_Jangid">github.com/TheRoboticsClub/gsoc2026-Kartik_Jangid</a>.</em></p>]]></content><author><name>Kartik Jangid</name></author><category term="gsoc"/><category term="community-bonding"/><category term="docker"/><category term="podman"/><category term="ros2"/><category term="robotics"/><category term="gsoc2026"/><category term="jderobot"/><summary type="html"><![CDATA[]]></summary></entry></feed>